Запускать main.py <название образа диска>
    Пример: sudo python3 /dev/sdb1
    Основные команды в командном итерпретаторе:
    cd <название директории> - перемещение по директории
    dir - список файлов и дерикторий в текущем каталоге
    info -p <f, b> информция о структуре FAT32
            f - информация о структуре FSINFO
            b - иноформация о стутуре BOOT_SECTOR
    cat <названия файла> -e <кодировка> - чтение указанного файла
            Кодировки:
                cp1251
                ascii
                utf-8
                bin - Двоичное чтения данных


Тестировалось:
    Верия Python:
        3.6.1
        3.5.2
    Верия Os:
      Windows 7(7600)
      Linux Lite 3.6(16.04)


Декомпозиция:
    commander.py ->
             Splitter  Разделяет стоку на список подсток
             Commander Командный итерпретатоор принимающий на вход файловый обзраз
    fat_struct.py ->
              FAT_BOOT_SECTOR Загрузочный сектор находится  в 1 секторе
              FAT_BOOT_FSINFO Дополнительная стутура по 2 сектору (необязательна)
              FAT_DIR_ENTRY Элемент в каталоге
              FAT_LFN - Структура для длинных именн

    file_system.py ->
              ElementTable - Элемент  в таблица каталог инкапсулируюший сложности FAT_DIR_ENTRY и FAT_LFN
              LogicalDisk - Логический диск с помощтю которого Commander общается c файловой системой FAT32

    main -> Запускает команндый интепретатор с переданным ему образом диска


    Сторки:
    string_cfg.py
    string.cgf

